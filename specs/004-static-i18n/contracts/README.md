# API Contracts README

Because this feature ("Static Dictionaries i18n") is entirely contained within the Astro build process and relies on static TypeScript objects for translations, there are no dynamic API endpoints, generic data contracts, or external web services added by this feature. No API contracts are needed.
\
